{
	"name": "salon-shop-render-api",
	"version": "0.1.0",
	"private": true,
	"description": "Render-ready API scaffold for the Salon Shop Supabase migration.",
	"main": "index.js",
	"type": "commonjs",
	"engines": {
		"node": "22.x"
	},
	"scripts": {
		"start": "node index.js",
		"dev": "node --watch index.js",
		"check": "node --check index.js && node --check src/config.js && node --check src/supabaseAdmin.js && node --check src/middleware/auth.js"
	},
	"dependencies": {
		"@supabase/supabase-js": "^2.76.1",
		"cors": "^2.8.5",
		"dotenv": "^17.2.3",
		"express": "^5.1.0",
		"resend": "^4.5.1"
	}
}
