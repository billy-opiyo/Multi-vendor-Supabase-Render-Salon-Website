# Root-level public migration notes.
# ------------------------------------------------------------
# The current frontend is static HTML/JS. Values in this file are NOT
# automatically injected into public/client-config.js unless a build step is
# added later. For now, copy public-safe values into public/client-config.js.
#
# Never commit real service-role keys or private API secrets.

SUPABASE_URL=https://YOUR_SUPABASE_PROJECT_REF.supabase.co
SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY
RENDER_BACKEND_URL=https://YOUR_RENDER_API_SERVICE.onrender.com