const NOTIFICATION_CHANNELS = Object.freeze({
	EMAIL: "email",
	WHATSAPP: "whatsapp",
	SMS: "sms",
	INTERNAL: "internal",
})

const NOTIFICATION_STATUSES = Object.freeze({
	PENDING: "pending",
	PROCESSING: "processing",
	SENT: "sent",
	FAILED: "failed",
	SKIPPED: "skipped",
	RETRYING: "retrying",
})

const NOTIFICATION_AGGREGATE_TYPES = Object.freeze({
	BOOKING: "booking",
	WAITLIST: "waitlist",
	REVIEW: "review",
	CONTACT_MESSAGE: "contact_message",
	SECURITY: "security",
	SYSTEM: "system",
})

const NOTIFICATION_TEMPLATE_KEYS = Object.freeze({
	BOOKING_CREATED: "booking.created",
	BOOKING_CONFIRMED: "booking.confirmed",
	BOOKING_CANCELLED: "booking.cancelled",
	BOOKING_RESCHEDULED: "booking.rescheduled",
	BOOKING_COMPLETED: "booking.completed",
	BOOKING_EXPIRED: "booking.expired",
	BOOKING_NO_SHOW: "booking.no_show",
	UPCOMING_BOOKING_REMINDER: "booking.upcoming_reminder",
	WAITLIST_JOINED: "waitlist.joined",
	WAITLIST_SLOT_OPEN: "waitlist.slot_open",
	CONTACT_MESSAGE_RECEIVED: "contact_message.received",
})

const BOOKING_STATUS_NOTIFICATION_TEMPLATES = Object.freeze({
	confirmed: NOTIFICATION_TEMPLATE_KEYS.BOOKING_CONFIRMED,
	cancelled: NOTIFICATION_TEMPLATE_KEYS.BOOKING_CANCELLED,
	completed: NOTIFICATION_TEMPLATE_KEYS.BOOKING_COMPLETED,
	expired: NOTIFICATION_TEMPLATE_KEYS.BOOKING_EXPIRED,
	no_show: NOTIFICATION_TEMPLATE_KEYS.BOOKING_NO_SHOW,
})

const DEFAULT_OUTBOX_LIMIT = 25
const DEFAULT_MAX_ATTEMPTS = 5
const DEFAULT_RETRY_BASE_SECONDS = 60
const DEFAULT_UPCOMING_REMINDER_LEAD_TIME_MINUTES = 2 * 60
const DEFAULT_UPCOMING_REMINDER_WINDOW_MINUTES = 15
const DEFAULT_EXPIRED_SLOT_GRACE_MINUTES = 120

module.exports = {
	BOOKING_STATUS_NOTIFICATION_TEMPLATES,
	DEFAULT_EXPIRED_SLOT_GRACE_MINUTES,
	DEFAULT_MAX_ATTEMPTS,
	DEFAULT_OUTBOX_LIMIT,
	DEFAULT_RETRY_BASE_SECONDS,
	DEFAULT_UPCOMING_REMINDER_LEAD_TIME_MINUTES,
	DEFAULT_UPCOMING_REMINDER_WINDOW_MINUTES,
	NOTIFICATION_AGGREGATE_TYPES,
	NOTIFICATION_CHANNELS,
	NOTIFICATION_STATUSES,
	NOTIFICATION_TEMPLATE_KEYS,
}
